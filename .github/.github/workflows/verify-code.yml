name: Fresh Start
on: [push] # This ensures it runs immediately on every commit

jobs:
  health-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Verify Connection
        run: echo "GitHub Actions is successfully rebuilt!"